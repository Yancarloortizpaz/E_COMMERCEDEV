# Reglas de Proyecto - E_COMMERCEDEV

- No analizar el proyecto completo. Solo basarse en `introducciones.md` y `nombres_y_sp_solo_los_paramtros.md`.
- No colocar ningún tipo de comentarios (0 comentarios) en ninguna de las creaciones de archivos de código.
- Mantenerse estrictamente dentro de la carpeta `C:\hector\E_COMMERCEDEV\API\API_ECCOMERCEDEV\`.
- Dividir las tareas en 2 bloques:
  1. Crear entidades (Domain Models) y DTOs, y detenerse para la revisión del usuario.
  2. Crear el resto (interfaces, repositorios, servicios, controladores, configuraciones) solo cuando el usuario lo indique explícitamente.
- Todo debe estar adaptado a los Stored Procedures (SPs) definidos en `nombres_y_sp_solo_los_paramtros.md`.
- Usar `DOMAIN.VariablesSalida.OUTPUT` como clase global para las variables de salida.
